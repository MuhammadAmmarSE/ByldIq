# ApproachStagePanel

The Byld IQ Approach timeline's (`ApproachTimeline`) side panel: what
happens, who participates, typical decisions, how AI assists, and what
success looks like for the active stage, plus its deliverables — the
same two-column shape the homepage's Product Thinking Timeline's
`StagePanel` uses, with different fields (CLAUDE.md Milestone 13 §4's
specific list, not Part 12's).
