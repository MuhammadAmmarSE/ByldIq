# infrastructure/

Deployment, monitoring, and cloud configuration (per CLAUDE.md Part 25:
`docker`, `terraform`, `nginx`, `cloudflare`, `aws`, `vercel`,
`monitoring`, `logging`, `backups`). Infrastructure never mixes with
application code.

Empty for now. Milestone 1 has no deployment target configured yet —
add infrastructure-as-code here once a hosting/deployment decision is
made, rather than guessing at it now.
