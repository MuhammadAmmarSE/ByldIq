# scripts/

Repository automation: build, clean, generate, audit, analyze, deploy
(per CLAUDE.md Part 25). Never business logic.

Empty for now — CI currently runs pnpm scripts directly; add a script
here when a task needs more than a one-line command.
