export interface KnowledgeContentTypePlaceholderProps {
  /** e.g. "whitepapers", "videos", "tutorials" — used in body copy. */
  contentTypeLabel: string;
  /** Why this content type has nothing published yet. */
  reason: string;
  className?: string;
}
