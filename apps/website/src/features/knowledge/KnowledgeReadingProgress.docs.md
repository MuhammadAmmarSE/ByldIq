# KnowledgeReadingProgress

CLAUDE.md Part 18's Reading Experience: reading progress. A thin bar fixed
to the top of the viewport (`z-fixed`, matching `Navbar`'s stacking
context) filled by `useReadingProgress`'s scroll-percentage calculation —
reuses the design system's real `Progress` primitive rather than a bespoke
bar.
