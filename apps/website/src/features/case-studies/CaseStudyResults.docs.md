# CaseStudyResults

CLAUDE.md Part 21's Results section: `approach`, `outcome`, and `metrics`
grouped into one component since all three describe what actually
happened, once the engineering was done.

## Metrics are never fabricated

Every metric comes from the authored values in `data/case-studies.ts`.
CLAUDE.md Part 7: "never fabricate numbers" — there is no synthetic
counter animation or invented precision here, only the values an author
put in the data file.

## Heading hierarchy

The section title renders as `<h2>` (`Heading variant="h3" as="h2"`).
