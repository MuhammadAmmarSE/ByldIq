# TargetUsersPanel

CLAUDE.md Milestone 14 §9's Target User capture. A simple CRUD list —
add/remove `TargetUserGroup` entries (`type`, `role`, `needs`,
`painPoints`, `goals`) — deliberately manual rather than AI-suggested;
unlike Discovery or Problem Definition, there isn't yet enough signal in
the answers to draft plausible user groups without guessing.
