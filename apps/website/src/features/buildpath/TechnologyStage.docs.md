# TechnologyStage

CLAUDE.md Milestone 14 §13's Technology Recommendations —
`mockAIProvider.generateTechnologyStack(answers)`, recomputed on every
render, never a bare technology name without an alternative and
reasoning. Cross-links to the real Technology Explorer via
`recommendedSlug` when the recommendation maps to a real entry there.
