# packages/

Reusable code shared across apps (per CLAUDE.md Part 25: `ui`, `icons`,
`animations`, `design-tokens`, `utils`, `hooks`, `config`, `types`,
`analytics`, `authentication`, `forms`, `charts`, `ai-sdk`, `search`,
`motion`, `theme`, ...).

Empty for now. `apps/website` is still the only app, so nothing has a
second consumer yet — per Part 27 ("promote only after multiple real
use cases"), code stays in `apps/website/src/` until a second app
actually needs to share it. Extract into a package here at that point,
not before.
