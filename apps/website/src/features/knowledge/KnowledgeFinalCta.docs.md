# KnowledgeFinalCta

CLAUDE.md Part 18's final CTA — the last section of the shared article
page template. Offers the same two paths as the hero (BuildPath, Talk to
Byld) plus a way back to `/knowledge`, mirroring `TechnologyFinalCta`'s
"multiple next steps" philosophy (Part 19) for visitors who read the
whole article without acting on the hero's CTA.

## Analytics

Tracks `knowledge_cta_selected` (`cta: "final-primary"` /
`"final-ai"`) and `knowledge_buildpath_started` — see `analytics.ts`.
