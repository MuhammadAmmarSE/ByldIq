# services/

Business-capability services (per CLAUDE.md Part 25: `ai`, `cms`, `search`,
`email`, `notifications`, `payments`, `storage`, `roadmap`, `proposal`,
`export`, `analytics`, `authentication`). Services own business logic,
never UI.

Empty for now — none of these capabilities exist yet (no AI backend, no
CMS, no payments). Add a service here when a real capability is built,
not as a placeholder for one that might exist later.
