# KnowledgeCoreConcepts

CLAUDE.md Part 18's Core Concepts section: the terms and ideas a reader
needs before the walkthrough and examples later in the article make sense
(`article.coreConcepts`), rendered as a definition list (`<dl>`) rather
than prose so it stays scannable.

## Heading hierarchy

The section title renders as `<h2>` (`Heading variant="h3" as="h2"`) —
`KnowledgeArticleHero`'s title owns the page's `<h1>`.
